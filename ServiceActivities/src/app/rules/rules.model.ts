export class Rule {
  public title: string;
  public content: string;
  public id: number;


  constructor(title: string, content: string, id: number) {
    this.title = title;
    this.content = content;
    this.id = id;
  }
}

